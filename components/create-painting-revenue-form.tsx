"use client";
import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export function CreatePaintingRevenueForm(){
 const r=useRouter(); const [description,setDescription]=useState(""); const [customer,setCustomer]=useState(""); const [amount,setAmount]=useState(""); const [date,setDate]=useState(""); const [error,setError]=useState("");
 async function submit(e:FormEvent){e.preventDefault();setError("");const res=await fetch("/api/painting-revenues",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({description,customer_name:customer,amount:Number(amount),painting_date:date||new Date().toISOString()})});const j=await res.json();if(!res.ok)setError(j.error||"Erro");else{setDescription("");setCustomer("");setAmount("");setDate("");r.refresh()}}
 return <form onSubmit={submit} className="card grid"><h2>Nova receita</h2>{error&&<div className="error">{error}</div>}<div className="field"><label>Serviço</label><input className="input" value={description} onChange={e=>setDescription(e.target.value)} required placeholder="Pintura de action figure"/></div><div className="field"><label>Cliente</label><input className="input" value={customer} onChange={e=>setCustomer(e.target.value)}/></div><div className="field"><label>Valor</label><input className="input" type="number" step="0.01" min="0.01" value={amount} onChange={e=>setAmount(e.target.value)} required/></div><div className="field"><label>Data</label><input className="input" type="date" value={date} onChange={e=>setDate(e.target.value)}/></div><button className="btn btn-primary">Salvar receita</button></form>;
}
