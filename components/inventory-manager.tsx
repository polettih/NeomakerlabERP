"use client";
import {useState} from 'react'; import {useRouter} from 'next/navigation';
export function InventoryManager({materials}:{materials:any[]}){const r=useRouter();const [form,setForm]=useState({name:'',category:'Insumos',unit:'un',minimum_stock:'0',supplier:''});const [purchase,setPurchase]=useState({material_id:materials[0]?.id||'',quantity:'',total_cost:'',supplier:''});const [use,setUse]=useState({material_id:materials[0]?.id||'',quantity:'',description:''});const [error,setError]=useState('');
async function post(url:string,body:any){const res=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await res.json();if(!res.ok)throw new Error(j.error||'Erro');}
async function create(e:any){e.preventDefault();try{await post('/api/materials',form);setForm({name:'',category:'Insumos',unit:'un',minimum_stock:'0',supplier:''});r.refresh()}catch(e:any){setError(e.message)}}
async function buy(e:any){e.preventDefault();try{await post('/api/material-purchases',purchase);setPurchase({...purchase,quantity:'',total_cost:''});r.refresh()}catch(e:any){setError(e.message)}}
async function consume(e:any){e.preventDefault();try{await post('/api/stock-movements',{...use,quantity:-Math.abs(Number(use.quantity)),movement_type:'manual_consumption'});setUse({...use,quantity:'',description:''});r.refresh()}catch(e:any){setError(e.message)}}
return <div className="grid two-col">{error&&<div className="error" style={{gridColumn:'1/-1'}}>{error}</div>}<form className="card grid" onSubmit={create}><h2>Novo material</h2><div className="field"><label>Nome</label><input className="input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></div><div className="form-grid"><div className="field"><label>Tipo</label><select className="select" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Insumos</option><option>Ferramentas</option><option>Maquinários</option></select></div><div className="field"><label>Unidade</label><input className="input" value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/></div><div className="field"><label>Estoque mínimo</label><input className="input" type="number" step="0.001" value={form.minimum_stock} onChange={e=>setForm({...form,minimum_stock:e.target.value})}/></div><div className="field"><label>Fornecedor</label><input className="input" value={form.supplier} onChange={e=>setForm({...form,supplier:e.target.value})}/></div></div><button className="btn btn-primary">Cadastrar material</button></form><form className="card grid" onSubmit={buy}><h2>Registrar compra</h2><div className="field"><label>Material</label><select className="select" value={purchase.material_id} onChange={e=>setPurchase({...purchase,material_id:e.target.value})}>{materials.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select></div><div className="form-grid"><div className="field"><label>Quantidade</label><input className="input" type="number" step="0.001" value={purchase.quantity} onChange={e=>setPurchase({...purchase,quantity:e.target.value})} required/></div><div className="field"><label>Valor total</label><input className="input" type="number" step="0.01" value={purchase.total_cost} onChange={e=>setPurchase({...purchase,total_cost:e.target.value})} required/></div></div><button className="btn btn-primary">Adicionar ao estoque</button></form><form className="card grid" onSubmit={consume}><h2>Gasto/consumo manual</h2><div className="field"><label>Material</label><select className="select" value={use.material_id} onChange={e=>setUse({...use,material_id:e.target.value})}>{materials.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select></div><div className="field"><label>Quantidade consumida</label><input className="input" type="number" step="0.001" min="0.001" value={use.quantity} onChange={e=>setUse({...use,quantity:e.target.value})} required/></div><div className="field"><label>Motivo</label><input className="input" value={use.description} onChange={e=>setUse({...use,description:e.target.value})} placeholder="Teste, impressão perdida, manutenção..."/></div><button className="btn btn-secondary">Baixar material</button></form><div className="card table-wrap" style={{gridColumn:'1/-1'}}><h2>Estoque atual</h2><table><thead><tr><th>Material</th><th>Tipo</th><th>Estoque</th><th>Mínimo</th><th>Custo médio</th><th>Status</th></tr></thead><tbody>{materials.map(m=><tr key={m.id}><td>{m.name}</td><td>{m.category}</td><td>{Number(m.quantity_on_hand).toFixed(3)} {m.unit}</td><td>{Number(m.minimum_stock).toFixed(3)} {m.unit}</td><td>R$ {Number(m.average_cost).toFixed(4)}</td><td><span className={`badge ${Number(m.quantity_on_hand)<=Number(m.minimum_stock)?'yellow':'green'}`}>{Number(m.quantity_on_hand)<=Number(m.minimum_stock)?'Estoque baixo':'Normal'}</span></td></tr>)}</tbody></table></div></div>}
