import {NextResponse} from 'next/server'; import {requireUser} from '@/lib/auth';
export async function POST(req:Request){try{const b=await req.json();if(!b.name)return NextResponse.json({error:'Nome é obrigatório.'},{status:400});const {supabase}=await requireUser();const {data:m}=await supabase.from('organization_members').select('organization_id').limit(1).single();if(!m)throw new Error('Organização não encontrada.');const materialType=b.material_type||'Outro'; const unit=materialType==='Filamento'?'g':materialType==='Resina'?'ml':(b.unit||'un'); if((materialType==='Filamento'||materialType==='Resina')&&!b.color_name)return NextResponse.json({error:'Informe a cor para filamento ou resina.'},{status:400}); const {data,error}=await supabase.from('materials').insert({organization_id:m.organization_id,name:b.name,category:b.category||'Insumos',material_type:materialType,color_name:b.color_name||null,color_hex:b.color_hex||null,unit,minimum_stock:Number(b.minimum_stock||0),supplier:b.supplier||null,notes:b.notes||null}).select().single();if(error)throw error;return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message||'Erro.'},{status:500})}}
export async function PATCH(req:Request){try{const b=await req.json();if(!b.id)return NextResponse.json({error:'ID obrigatório.'},{status:400});const {supabase}=await requireUser();const update:any={};for(const k of ['name','category','material_type','color_name','color_hex','unit','minimum_stock','supplier','notes','active'])if(b[k]!==undefined)update[k]=k==='minimum_stock'?Number(b[k]):b[k];const {data,error}=await supabase.from('materials').update(update).eq('id',b.id).select().single();if(error)throw error;return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message||'Erro.'},{status:500})}}
