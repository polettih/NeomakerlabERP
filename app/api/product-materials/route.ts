import {NextResponse} from 'next/server'; import {requireUser} from '@/lib/auth';
export async function POST(req:Request){try{const b=await req.json();const {supabase}=await requireUser();const {data,error}=await supabase.from('product_materials').upsert({product_id:b.product_id,material_id:b.material_id,quantity:Number(b.quantity),notes:b.notes||null},{onConflict:'product_id,material_id'}).select().single();if(error)throw error;return NextResponse.json(data)}catch(e:any){return NextResponse.json({error:e.message||'Erro.'},{status:500})}}
export async function DELETE(req:Request){try{const b=await req.json();const {supabase}=await requireUser();const {error}=await supabase.from('product_materials').delete().eq('id',b.id);if(error)throw error;return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message||'Erro.'},{status:500})}}
