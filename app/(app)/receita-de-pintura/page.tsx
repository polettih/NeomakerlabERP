import {requireUser} from "@/lib/auth";
import {CreatePaintingRecipeForm} from "@/components/create-painting-recipe-form";
import {RecipeTemplateButton} from "@/components/recipe-template-button";
export default async function ReceitaPinturaPage(){const {supabase}=await requireUser();const [{data:recipes},{data:templates},{data:products}]=await Promise.all([supabase.from("painting_recipes").select("id,name,category,description,colors,dilution,finish,notes,created_at,products(name,product_images(public_url,sort_order))").order("created_at",{ascending:false}),supabase.from("painting_recipe_templates").select("id,name,category,description,colors,dilution,finish,notes").order("category").order("name"),supabase.from("products").select("id,name").order("name")]);return <div className="content"><div className="section-title"><div><h1>Receita de pintura</h1><p className="muted">Biblioteca de misturas e processos para repetir suas pinturas com consistência.</p></div></div><div className="grid two-col"><CreatePaintingRecipeForm products={products??[]}/><div className="grid"><div className="card"><h2>Receitas iniciais</h2><p className="muted">São pontos de partida. Ajuste conforme a marca da tinta, aerógrafo/pincel e referência.</p><div className="grid">{(templates??[]).map(t=><div key={t.id} className="card" style={{padding:14}}><div className="section-title"><div><strong>{t.name}</strong><div className="muted">{t.category}</div></div><RecipeTemplateButton template={t}/></div><p>{t.description}</p><div className="muted"><strong>Cores:</strong> {(t.colors??[]).join(", ")}</div>{t.dilution&&<div className="muted"><strong>Diluição:</strong> {t.dilution}</div>}{t.finish&&<div className="muted"><strong>Acabamento:</strong> {t.finish}</div>}</div>)}</div></div></div></div><div className="card"><h2>Minha biblioteca</h2><div className="table-wrap"><table><thead><tr><th>Receita</th><th>Categoria</th><th>Produto</th><th>Cores</th><th>Acabamento</th></tr></thead><tbody>{(recipes??[]).map((x:any)=><tr key={x.id}><td><strong>{x.name}</strong><div className="muted">{x.description||""}</div></td><td>{x.category}</td><td><div className="product-name-cell">{x.products?.product_images?.sort((a:any,b:any)=>a.sort_order-b.sort_order)?.[0]?.public_url?<img className="product-thumb" src={x.products.product_images.sort((a:any,b:any)=>a.sort_order-b.sort_order)[0].public_url} alt=""/>:null}<span>{x.products?.name||"-"}</span></div></td><td>{(x.colors??[]).join(", ")}</td><td>{x.finish||"-"}</td></tr>)}{!recipes?.length&&<tr><td colSpan={5} className="muted">Sua biblioteca ainda está vazia. Adicione uma receita inicial ou crie a sua.</td></tr>}</tbody></table></div></div></div>}
