import {requireUser} from "@/lib/auth"; import {ChannelManager} from "@/components/channel-manager";
export default async function ConfiguracoesPage(){const {supabase}=await requireUser(); const {data}=await supabase.from("sales_channels").select("id,name,active").order("name"); return <div className="content"><div className="section-title"><div><h1>Configurações</h1><p className="muted">Gerencie os canais usados para registrar pedidos.</p></div></div><ChannelManager channels={data??[]}/></div>}
