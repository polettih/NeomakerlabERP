import {requireUser} from "@/lib/auth"; import Link from "next/link"; import {OrderStatusActions} from "@/components/order-status-actions";
const money=(v:number)=>v.toLocaleString("pt-BR",{style:"currency",currency:"BRL"});
export default async function PedidosPage(){const {supabase}=await requireUser(); const {data}=await supabase.from("orders").select("id,status,payment_status,total,order_date,expected_date,customers(name),sales_channels(name)").order("order_date",{ascending:false}); return <div className="content"><div className="section-title"><div><h1>Pedidos</h1><p className="muted">Acompanhe e altere o status de cada pedido.</p></div><Link className="btn btn-primary" href="/pedidos/novo">+ Novo pedido</Link></div><div className="table-wrap"><table><thead><tr><th>Data</th><th>Cliente</th><th>Canal</th><th>Prazo</th><th>Status</th><th>Pagamento</th><th>Total</th></tr></thead><tbody>{(data??[]).map((o:any)=><tr key={o.id}><td>{new Date(o.order_date).toLocaleDateString("pt-BR")}</td><td>{o.customers?.name||"—"}</td><td>{o.sales_channels?.name||"—"}</td><td>{o.expected_date?new Date(o.expected_date).toLocaleDateString("pt-BR"):"—"}</td><td><OrderStatusActions id={o.id} status={o.status}/></td><td><span className="badge">{o.payment_status}</span></td><td>{money(Number(o.total))}</td></tr>)}{!data?.length&&<tr><td colSpan={7} className="muted">Nenhum pedido cadastrado.</td></tr>}</tbody></table></div></div>}
